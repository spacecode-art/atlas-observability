name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  secrets-scan:
    permissions:
      contents: read
      pull-requests: write
    uses: spacecode-art/atlas-security/.github/workflows/reusable-secrets-scan.yml@v1.0.4
    secrets: inherit